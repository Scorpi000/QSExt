# -*- coding: utf-8 -*-
"""
StrategyDef 配置模板

复制此文件为 settings.py 并修改实际值。
settings_local.py（不纳入版本管理）可以覆盖此文件的任何变量。

支持的环境变量（STRATEGYDEF_ 前缀）：
  STRATEGYDEF_DEBUG=true
  STRATEGYDEF_END_DT="2026-06-30"
  STRATEGYDEF_WORKERS=4
"""

import os

# ============================================================
# 运行模式
# ============================================================
DEBUG = False
UPDATE_DATA = True
REGISTER_GRAPH = False
DRY_RUN = False

# ============================================================
# 因子数据库配置
# ============================================================
FACTOR_DATABASES = [
    {
        "name": "JYDB",
        "class": "JYDB",
        "role": "source",
        "args": {
            "FTArgs": {"PreFilterID": False},
        },
    },
    {
        "name": "TDB",
        "class": "HDF5DB",
        "role": "target",
        "args": {
            "MainDir": r"D:\Data\HDF5DB",
        }
    },
]

# ============================================================
# 时间与 ID 数据源
# ============================================================
DT_SOURCE = "JYDB"
ID_SOURCE = "JYDB"

# ============================================================
# 时间范围
# ============================================================
END_DT = "last_friday"
START_DT = None
LOOKBACK = 15
MAX_LOOKBACK = 365 * 10
DT_TYPE = "交易日"      # 交易日 | 自然日
DT_FREQ = "1d"          # 1d | 1w | 2w | 1m | 1q | 1y

# ============================================================
# ID 类型与策略模块
# ============================================================
ID_PROFILES = [
    {
        "id_type": "A股",
        "id_selection": {"type": "all"},
        "strategy_modules": [
            "QSExt.StrategyDef.example_strategy",
        ],
    },
]

# ============================================================
# 输出
# ============================================================
TARGET_DB = "TDB"

# ============================================================
# 执行
# ============================================================
WORKERS = 8

# ============================================================
# 图数据库
# ============================================================
NEO4J_CONFIG_PATH = "~/QuantStudioConfig/Neo4jDBConfig.json"
EMBEDDING_MODEL = "bge-m3"
EMBEDDING_DIM = 1024
SKIP_EMBEDDING = False

# ============================================================
# 日志
# ============================================================
LOG_LEVEL = "DEBUG"
